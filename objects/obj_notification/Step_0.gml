// ==========================================
// STICK TO PLAYER
// ==========================================
if (instance_exists(obj_player)) {
    x = obj_player.x;
    y = obj_player.y - 100; // Hover exactly 100 pixels above the cat
}